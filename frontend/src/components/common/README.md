# Common Components

Generic, reusable UI primitives live here.

Examples (to be added per-phase):

- `Button.tsx` — Generic button with variants
- `Card.tsx` — Content container
- `DataTable.tsx` — Sortable, filterable table
- `FilterBar.tsx` — Search + filter controls
- `MetricCard.tsx` — KPI display with trend
- `RiskBadge.tsx` — Color-coded score indicator
